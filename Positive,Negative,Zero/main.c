#include <stdio.h>
#include <stdlib.h>


int main()
{
    int a;
        printf("Enter integer number");
        scanf("%d",&a);
    if(a>0);
       {printf("The given integer number is positive number.");}
    if(a<0);
        {printf("The given integer number is negative number. ");}
        if(a==0);
        {printf("The given number is neither positive nor negative.");}
    return 0;
}
